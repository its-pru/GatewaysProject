package datasource;

public class ChemicalTableDataGateway {
	/**
	 *          One class with a static methods for:
	 * Finding row by ID
	 * Updating a row by ID
	 * Inserting a row into the table
	 * Deleting a row by ID
	 * Get the record set
	 */
}
